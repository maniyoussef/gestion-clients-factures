package com.example;  // Make sure this matches the package in your pom.xml

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class GestionClientsFacturesApplication {

    public static void main(String[] args) {
        SpringApplication.run(GestionClientsFacturesApplication.class, args);
    }
}
